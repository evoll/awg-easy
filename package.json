{
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up awg-easy --build",
    "cli:dev": "docker compose -f docker-compose.dev.yml run --build --rm -it awg-easy cli:dev",
    "build": "docker build -t awg-easy .",
    "docs:preview": "docker run --rm -it -p 8080:8080 -v ./docs:/docs squidfunk/mkdocs-material serve -a 0.0.0.0:8080",
    "scripts:version": "bash scripts/version.sh",
    "format:check:docs": "prettier --check docs"
  },
  "devDependencies": {
    "prettier": "^3.7.3"
  },
  "packageManager": "pnpm@10.19.0"
}
